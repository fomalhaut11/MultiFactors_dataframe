# 开发工具

本目录包含项目开发和调试所需的各种工具，提高开发效率和代码质量。

## 📁 目录结构

### helpers/ - 辅助工具
- `subagent_manager.py` - 子代理管理器
- `agent_helper.py` - 智能代理辅助工具

### debug/ - 调试工具
- `check_factor_format.py` - 因子格式检查工具
- `debug_index_format.py` - 索引格式调试工具  
- `check_financial_data_structure.py` - 财务数据结构检查

## 🔧 工具使用说明

### 调试工具

#### 因子格式检查
```bash
# 检查因子数据格式是否正确
python tools/debug/check_factor_format.py --factor ROE_ttm
```

#### 索引格式调试
```bash
# 调试MultiIndex格式问题
python tools/debug/debug_index_format.py --data-file /path/to/factor.pkl
```

#### 财务数据结构检查
```bash
# 检查财务数据结构完整性
python tools/debug/check_financial_data_structure.py
```

### 辅助工具

#### 代理管理
```bash
# 管理智能代理配置
python tools/helpers/subagent_manager.py --list-agents
python tools/helpers/subagent_manager.py --create-agent MyAgent
```

#### 代理辅助
```bash
# 代理开发辅助功能
python tools/helpers/agent_helper.py --help
```

## 🎯 工具开发指南

### 添加新工具
1. 根据功能类型选择合适的子目录(`helpers/`或`debug/`)
2. 创建独立的Python脚本
3. 添加命令行参数支持
4. 包含详细的帮助信息
5. 更新本README文件

### 工具编写规范
```python
#!/usr/bin/env python3
"""
工具名称 - 简要描述

详细功能描述...
"""

import argparse
import logging

def main():
    parser = argparse.ArgumentParser(description='工具描述')
    parser.add_argument('--param', help='参数说明')
    args = parser.parse_args()
    
    # 工具逻辑
    pass

if __name__ == '__main__':
    main()
```

## 🐛 常见调试场景

### 数据格式问题
- 使用`check_factor_format.py`检查因子数据格式
- 使用`debug_index_format.py`调试MultiIndex问题

### 数据完整性问题  
- 使用`check_financial_data_structure.py`验证财务数据
- 检查数据字段映射是否正确

### 开发效率工具
- 使用代理相关工具加速开发流程
- 利用自动化工具减少重复工作

## ⚠️ 使用注意事项

1. **数据安全**: 调试工具可能输出敏感信息，注意数据保护
2. **性能影响**: 某些工具可能消耗较多资源，避免在生产环境使用
3. **版本兼容**: 工具可能依赖特定的数据格式版本
4. **工具维护**: 定期检查工具是否需要更新

---
*工具仅供开发调试使用，请勿在生产环境直接运行*